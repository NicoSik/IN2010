public class Selectionsort extends Sorter {

    @Override
    void sort() {
        int len = A.length;
        for (int i = 0; i < len; i++) {
            int j = i;
            int sml = A[j];
            for ()
        }
    }

    @Override
    String algorithmName() {
        return "Selectionsort";
    }
}